apiVersion: v1
kind: Service
metadata:
  name: {{ include "orean-backend.fullname" . }}
  labels:
    {{- include "orean-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "orean-backend.selectorLabels" . | nindent 4 }}
